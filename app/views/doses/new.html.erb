<h1>Add a dose</h1>
<%= simple_form_for [@cocktail, @dose] do |f| %>
<%= f.association :ingredient, prompt: "Choose an ingredient"%>
<%= f.input :description %>
<%= f.submit "Add a dose to #{@cocktail.name}", class: "btn btn-primary" %>
<% end %>

